import { JsonCustomConvert } from "json2typescript";
export declare class DateConverter implements JsonCustomConvert<Date> {
    serialize(data: Date): Date;
    deserialize(enumType: string): Date;
}

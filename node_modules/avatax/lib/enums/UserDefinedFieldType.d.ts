import { JsonCustomConvert } from "json2typescript";
/**
* @export
* @enum {string}
*/
export declare enum UserDefinedFieldType {
    Document = 1,
    Line = 2
}
export declare class UserDefinedFieldTypeConverter implements JsonCustomConvert<UserDefinedFieldType> {
    serialize(data: UserDefinedFieldType): UserDefinedFieldType;
    deserialize(enumType: string): UserDefinedFieldType;
}

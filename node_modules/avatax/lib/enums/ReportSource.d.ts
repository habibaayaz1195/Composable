import { JsonCustomConvert } from "json2typescript";
/**
* @export
* @enum {string}
*/
export declare enum ReportSource {
    SNOWFLAKE = 0,
    MONGODB = 1
}
export declare class ReportSourceConverter implements JsonCustomConvert<ReportSource> {
    serialize(data: ReportSource): ReportSource;
    deserialize(enumType: string): ReportSource;
}

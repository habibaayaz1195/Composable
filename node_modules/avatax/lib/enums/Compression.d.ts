import { JsonCustomConvert } from "json2typescript";
/**
* @export
* @enum {string}
*/
export declare enum Compression {
    NONE = 0,
    GZIP = 1
}
export declare class CompressionConverter implements JsonCustomConvert<Compression> {
    serialize(data: Compression): Compression;
    deserialize(enumType: string): Compression;
}

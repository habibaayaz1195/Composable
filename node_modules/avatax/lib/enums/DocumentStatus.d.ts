import { JsonCustomConvert } from "json2typescript";
/**
* @export
* @enum {string}
*/
export declare enum DocumentStatus {
    Temporary = 0,
    Saved = 1,
    Posted = 2,
    Committed = 3,
    Cancelled = 4,
    Adjusted = 5,
    Queued = 6,
    PendingApproval = 7,
    Any = -1
}
export declare class DocumentStatusConverter implements JsonCustomConvert<DocumentStatus> {
    serialize(data: DocumentStatus): DocumentStatus;
    deserialize(enumType: string): DocumentStatus;
}

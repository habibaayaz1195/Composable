import { JsonCustomConvert } from "json2typescript";
/**
* @export
* @enum {string}
*/
export declare enum SeverityLevel {
    Success = 0,
    Warning = 1,
    Error = 2,
    Exception = 3
}
export declare class SeverityLevelConverter implements JsonCustomConvert<SeverityLevel> {
    serialize(data: SeverityLevel): SeverityLevel;
    deserialize(enumType: string): SeverityLevel;
}

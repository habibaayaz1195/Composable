import { JsonCustomConvert } from "json2typescript";
/**
* @export
* @enum {string}
*/
export declare enum POABusinessUnit {
    Returns = 0,
    Lodging = 1,
    Telecom = 2
}
export declare class POABusinessUnitConverter implements JsonCustomConvert<POABusinessUnit> {
    serialize(data: POABusinessUnit): POABusinessUnit;
    deserialize(enumType: string): POABusinessUnit;
}

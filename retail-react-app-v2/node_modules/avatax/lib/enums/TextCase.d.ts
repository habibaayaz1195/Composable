import { JsonCustomConvert } from "json2typescript";
/**
* @export
* @enum {string}
*/
export declare enum TextCase {
    Upper = 0,
    Mixed = 1
}
export declare class TextCaseConverter implements JsonCustomConvert<TextCase> {
    serialize(data: TextCase): TextCase;
    deserialize(enumType: string): TextCase;
}
